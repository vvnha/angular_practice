import { ColorbandDirective } from './colorband.directive';

describe('ColorbandDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorbandDirective();
    expect(directive).toBeTruthy();
  });
});
